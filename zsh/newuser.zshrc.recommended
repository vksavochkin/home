#!/bin/zsh
# ~/.zshrc
### Загружаем общие настройки.
# Можно скопировать содержимое файла сюда, а его подключение убрать.
if test -r /etc/zsh/zshrc.include; then
    . /etc/zsh/zshrc.include
fi

### Добавляем свои настройки.

